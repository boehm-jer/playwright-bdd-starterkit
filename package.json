{
  "scripts": {
    "test": "npx bddgen && npx playwright test",
    "ui": "npx bddgen && npx playwright test --ui",
    "report": "npx playwright show-report",
    "tags-test": "bash scripts/tags-test.sh",
    "setup-hooks": "ln -sf \"$(pwd)/scripts/pre-commit.sh\" .git/hooks/pre-commit && echo 'Git hooks installed'"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.11.1",
    "@playwright/test": "^1.58.1",
    "@types/node": "^25.6.0",
    "pdf-parse": "^2.4.5",
    "playwright-bdd": "^8.4.2"
  },
  "engines": {
    "node": ">=20.16.0"
  }
}
